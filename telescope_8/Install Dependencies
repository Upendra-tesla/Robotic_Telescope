# Core dependencies
sudo apt update && sudo apt install -y python3-pyqt5 python3-gpiozero python3-pip
pip3 install picamera2 pandas openpyxl ephem matplotlib requests numpy

Before running the app, start the Ollama server (required for local AI):
bash
Run
ollama serve &
ollama run deepseek-r1:1.5b  # Keep this terminal open